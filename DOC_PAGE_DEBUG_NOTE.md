# 文档页首次进入较慢排查记录

## 现象

- 从首页点击进入 `/documents/[documentId]` 时，第一次进入明显较慢
- 连续第二次进入同一个文档会快很多

## 排查结论

- 主要瓶颈不是编辑器初始化
- 主要耗时发生在 `next dev` 开发环境下的首次路由编译
- 其次还有服务端首屏请求耗时：
  - Clerk `getToken()` 约 `500ms`
  - Convex `preloadQuery()` 约 `800ms - 900ms`

## 日志结论摘要

- 点击后会先看到 `Fast Refresh rebuilding`
- 该阶段大约耗时 `5s - 6s`
- 编译完成后，服务端才真正开始执行文档页加载逻辑

## 当前判断

- 这是开发环境首次按需编译导致的慢，不是主要业务逻辑缺陷
- `editor.tsx` 不是当前核心瓶颈

## 建议

- 用 `npm run build` + `npm run start` 再验证一次生产模式体验
- 如果只在 `dev` 慢，可视为开发环境现象
- 如果后续要继续优化开发体验，可优先考虑拆分文档页首屏客户端组件
